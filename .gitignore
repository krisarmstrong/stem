
!certs/.gitkeep
# AI tool artifacts — never tracked.
.claude/
.cursor/
.gemini/
# Local session/spec workspace — never tracked.
_project_specs/
.env
.env.*.local
.env.local
.gemini/
.gocache/
.gomodcache/
.tmp_*
*.a
*.cmd
*.crt
*.db
*.deb
*.dll
*.dylib
*.exe
*.exe~
*.key
*.lib
*.log
*.o
*.obj
*.out
*.pem
*.rpm
*.so
*.sqlite
*.sqlite3
*.tar.gz
*.test
*.tmp
*.tsbuildinfo
*.zip
*~
/bin/
/build/
/dist/
/stem
/stem-*
# ╔═══════════════════════════════════════════════════════════════════════════╗
# ─── AI/Agent Artifacts ──────────────────────────────────────────────────────
# ─── Application Binaries ────────────────────────────────────────────────────
# ─── Build & Package Artifacts ───────────────────────────────────────────────
# ─── Build Directories ───────────────────────────────────────────────────────
# ─── C/DPDK Build Artifacts ──────────────────────────────────────────────────
# ─── Configuration Overrides ─────────────────────────────────────────────────
# ─── Database & Runtime Data ─────────────────────────────────────────────────
# ─── Debug & Development ─────────────────────────────────────────────────────
# ─── Environment & Secrets ───────────────────────────────────────────────────
# ─── Frontend (UI) ───────────────────────────────────────────────────────────
# ─── Go Testing & Coverage ───────────────────────────────────────────────────
# ─── Go Workspace & Dependencies ─────────────────────────────────────────────
# ─── Logs ────────────────────────────────────────────────────────────────────
# ║ Network performance testing and orchestration platform                    ║
# ║ Stack: Go, C (DPDK), TypeScript/React (UI), Playwright                   ║
# ║ Stem - .gitignore                                                         ║
# ─── Temporary Files ─────────────────────────────────────────────────────────
# ─── Test Results ────────────────────────────────────────────────────────────
# AI tool session data
# Auto-generated/runtime data
# Build outputs
# Certificates (generated at runtime)
# Debug output
# Dependencies
# DPDK-specific
# Generated headers
# Local overrides (defaults should be version controlled)
# Logs
# Playwright test results
# TypeScript
certs/
CLAUDE.md
config.yaml
configs/*.local.json
configs/*.local.yaml
core.*
coverage.html
coverage.txt
data/
!ui/src/data/
DebugImages/
go.work
go.work.sum
include/version_generated.h
logs/
node_modules/
npm-debug.log*
reports/
results/
rfc2544-linux
seedtest
temp/
tests/results/
tmp/
ui/.vite/
ui/coverage/
ui/dist/
ui/node_modules/
ui/playwright-report/
ui/playwright/.auth/

# Frontend build outputs (Vite builds directly to internal/api/ui)
internal/api/ui/*
!internal/api/ui/.gitkeep
# Legacy embed output (migrated to internal/api/ui/)
internal/api/dist/
ui/test-results/
vendor/
yarn-error.log*

# Build output
dist/
*.coverage.out
