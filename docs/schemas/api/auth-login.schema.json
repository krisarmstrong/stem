{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/krisarmstrong/stem/main/docs/schemas/api/auth-login.schema.json",
  "$ref": "#/$defs/AuthLoginRequest",
  "$defs": {
    "AuthLoginRequest": {
      "properties": {
        "username": {
          "type": "string"
        },
        "password": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "username",
        "password"
      ]
    }
  },
  "title": "AuthLoginRequest",
  "description": "AuthLoginRequest — generated from the Go struct in internal/api; refresh with `make schema` after struct changes."
}
